import { Routes } from '@angular/router';
import { DetailTicketsComponent } from './detail-tickets.component';

export default [
    { path: '', component: DetailTicketsComponent },
    
] as Routes;
