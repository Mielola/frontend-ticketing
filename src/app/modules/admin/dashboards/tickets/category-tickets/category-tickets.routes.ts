import { Routes } from '@angular/router';
import { CategoryTicketsComponent } from './category-tickets.component';

export default [
    {
        path: '',
        component: CategoryTicketsComponent,
    },
] as Routes;
